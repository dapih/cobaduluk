<!--
TEMPLATE: log report. Copy to output/<job-id>/log-<job-id>.md and fill in.
This is the running record of the job. Append entries chronologically; never
rewrite history. One line per milestone, result, change, or decision.
Entry format:  - [YYYY-MM-DD HH:MM] [step] message
-->
# Log: table-20260628-2

**Source:** kkp2.xlsx
**Sheet / table:** Sheet1
**Started:** 2026-06-28 (local)
**Status:** done  <!-- in-progress | done | blocked -->

## Milestones
- [2026-06-28] [new-job] Created job folder `output/table-20260628-2/`; copied input as `table-20260628-2.xlsx`.
- [2026-06-28] [inspect] Ran inspect_xlsx.py; 298 rows x 8 cols, 30 blank-row gaps, 0 merged cells.
- [2026-06-28] [mapping] Mapping proposal drafted (see Decisions below). Awaiting user confirmation before schema authoring.
- [2026-06-28] [schema] Authored table-20260628-2.schema.json — sections wrapper, recursive ListItem, enums for jangka_waktu_penerbitan and kewenangan.

## Decisions

### 2026-06-28 — Column→Field Mapping Proposal (PENDING CONFIRMATION)

**Header row:** 2. **Data start row:** 4.

#### Row classification

| Row type | Detection rule | Action |
|---|---|---|
| Blank separator | All 8 cells empty | Skip entirely |
| Section header | Col A non-empty AND matches `^[IVX]+\.` (Roman numeral) AND B–H all empty | Emit as section group; do NOT start an item entry |
| Item row (entry boundary) | Col A non-empty AND matches `^\d+\.` (Arabic numeral) | Start new entry; harvest B–H first-row values |
| Continuation row | Col A empty | Accumulate C, E, G list items into current entry |

Note: Col A has 25 non-empty values and 25 distinct values. All are unique, so the Roman numeral section headers and Arabic item numbers are at the same column. Sections must be discriminated by pattern match, not just non-emptiness.

#### Column→Field mapping

| Col | Header | Fill rate | Role | JSON field name | Type | Notes |
|---|---|---|---|---|---|---|
| A | No | 0.084 | entry boundary + section key | `no` / `section` | string | Section rows: store value in `section.title`; item rows: store in `entry.no` |
| B | Nomeklatur PB UMKU | 0.095 | header attribute (one per entry) | `nama_pb_umku` | string | Populated on item row first row only; 5 occurrences of `(*berlaku untuk seluruh KBLI)` — likely a note/qualifier sub-value; see OQ-1 |
| C | Persyaratan | 0.672 | detail list (multi-row) | `persyaratan` | array of objects | Numbered `1.`/`a.`/`b.` — nest with `nest_by_pattern` |
| D | Jangka Waktu Penerbitan | 0.084 | header attribute (one per entry) | `jangka_waktu_penerbitan` | string | 7 distinct values; candidate enum — see Enums section |
| E | Kewajiban | 0.378 | detail list (multi-row) | `kewajiban` | array of objects | Numbered `1.`/`a.`/`b.` — nest with `nest_by_pattern` |
| F | Masa Berlaku | 0.084 | header attribute (one per entry) | `masa_berlaku` | string | 7 distinct values; candidate enum — see Enums section; contains soft-hyphen artifact |
| G | Parameter | 0.311 | detail list (multi-row) | `parameter` | array of objects | Mixed: some items are plain strings (`Seluruh`), some numbered `1.`/`2.`/`a.` — nest with `nest_by_pattern`; see OQ-2 |
| H | Kewenangan | 0.108 | header attribute (one per entry) | `kewenangan` | string | 3 distinct values; strong enum candidate; case variant `Kepala badan` vs `Kepala Badan` — normalize |

#### Hierarchy model

```
root (array)
└── section          (driven by Col A Roman-numeral row)
    ├── section_id   e.g. "I", "II"
    ├── section_title e.g. "KELAYAKAN OPERASI (B)"
    └── items (array)
        └── entry    (driven by Col A Arabic-numeral row)
            ├── no                        (Col A)
            ├── nama_pb_umku              (Col B, first row of entry)
            ├── jangka_waktu_penerbitan   (Col D, first row)
            ├── masa_berlaku              (Col F, first row)
            ├── kewenangan                (Col H, first row)
            ├── persyaratan               (Col C, accumulated + nested)
            ├── kewajiban                 (Col E, accumulated + nested)
            └── parameter                 (Col G, accumulated + nested)
```

#### Multi-level numbering — `nest_by_pattern` config

All three detail columns (C, E, G) show two visible levels in samples:
- Level 1: `^\d+\.`  (e.g. `1.`, `2.`, `3.`)
- Level 2: `^[a-z]\.`  (e.g. `a.`, `b.`, `c.`)
- Level 3 (possible): `^\d+\)`  — not confirmed in samples; flag as OQ-3

Proposed call (same pattern set for C and E; G may differ — see OQ-2):
```python
nest_by_pattern(
    flat_list,
    levels=[("num", r"^\d+\."), ("alpha", r"^[a-z]\.")],
    drop=["-"],
    orphan="attach",
)
```

#### Candidate enums

| Field | Distinct values |
|---|---|
| `jangka_waktu_penerbitan` | `4 Hari`, `7 Hari`, `10 Hari`, `14 Hari`, `15 Hari`, `21 Hari`, `30 Hari` |
| `masa_berlaku` | `1 tahun`, `2 tahun`, `4 tahun`, `5 tahun`, `10 tahun`, `1 tahun musim penang-kapan ikan`, `Selama masa berlaku perizinan berusaha` |
| `kewenangan` | `Menteri/ Kepala Badan`, `Gubernur` (normalize case variant) |

#### Normalization flags

| Artifact | Example | Column | Action |
|---|---|---|---|
| Soft-hyphen word split | `penang-kapan ikan`, `berukur-an`, `mene-ngah` | F, G | Safe auto-clean: `dehyphenate()` — remove hyphens at syllable breaks. Confirm before applying to proper nouns. |
| Case variant | `Kepala badan` vs `Kepala Badan` | H | Normalize to `Menteri/ Kepala Badan` (title case form) |
| Dash placeholder `-` | Values in G like `b. Kawa-san Strate-gis Nasio-nal` | G | These are hyphenated words, not placeholders — apply `dehyphenate()`; do NOT drop |
| Note qualifier in B | `(*berlaku untuk seluruh KBLI)` appears 5× | B | See OQ-1 |
| Col H fill rate 0.108 > 0.084 | 32 non-empty vs 25 items | H | OQ-4: H may populate on continuation rows too — verify |

### Open Questions (must be answered before schema authoring)

**OQ-1 — Col B note qualifier:** `(*berlaku untuk seluruh KBLI)` appears 5 times in Col B alongside normal license names. Are these:
  - (a) a separate sub-field `kbli_note` on the entry, or
  - (b) a suffix appended to `nama_pb_umku`, or
  - (c) a row of its own that should be treated as a B-level continuation?
  Fill rate of B (0.095 = 28/296) is higher than Col A (25/296), confirming B is sometimes populated on non-boundary rows. This is the likely cause.

**OQ-2 — Col G `parameter` structure:** G contains both plain single values (`Seluruh`) and numbered lists (`1.`/`2.`/`a.`). Should:
  - (a) plain values be kept as a single string scalar (no nesting), while numbered values get `nest_by_pattern`, or
  - (b) all G values always be modeled as an array (even singletons)?

**OQ-3 — Third numbering level in C/E:** Do cols C or E ever use `1)` paren style as a third level (not visible in the 10-row sample)? If yes, add `("paren", r"^\d+\)")` to `nest_by_pattern` levels.

**OQ-4 — Col H Kewenangan fill rate mismatch:** Col H has 32 non-empty values but Col A has only 25 items (including section headers). This means H is populated on ~7 continuation rows. Is H a per-entry constant that repeats across parameter sub-rows, or is `kewenangan` actually per-parameter-value (i.e. different rows within one entry can have different `kewenangan`)?

**OQ-5 — Section model in output:** Should Roman-numeral section headers appear in the JSON output as wrapper objects, or should each entry carry a flat `section` string field? (Wrapper objects = array-of-sections structure; flat field = array-of-entries where each entry has `"section": "I. KELAYAKAN OPERASI (B)"`.)

**OQ-6 — Col A value `(*berlaku untuk seluruh KBLI)` in B:** Related to OQ-1 — are there any Col A rows with asterisk-prefixed values (like `*Surat Izin Membangun...` seen in B's top_values)? Or do those asterisks belong strictly to B? Inspect samples don't show an asterisk in A; confirm.

## Schema summary

Root is an array of `Section` objects (driven by Roman-numeral Col A rows), each containing an `items` array of `Entry` objects (driven by Arabic-numeral Col A rows). Three array fields — `persyaratan`, `kewajiban`, `parameter` — share the recursive `ListItem` def with `{ label, text, children[] }` supporting three nesting levels (`1.` / `a.` / `1)`). Two enums are enforced: `jangka_waktu_penerbitan` (7 values, Hari) and `kewenangan` (2 values); `masa_berlaku` is kept open string pending review; `kbli_note` is `["string","null"]`; `applies_to_all_kbli` is boolean default false.

## Changes
<!-- Every edit to schema/instance/parser worth noting. -->
- [2026-06-28] [refactor] Adopt the `kkp-licensing` family-canonical list idiom: parser now emits `{teks, sub}` directly (dropped the `convert_nodes` step that produced `{label, text, children}`); schema's `ListItem` def replaced by the canonical `Item {teks, sub}`. Also fixed stale absolute paths (the committed parser pointed at a `…/cobaduluk/…` location) to resolve from the file like the table-20260628-1 parser. Re-ran: 266 rows / 22 entries / 33 tiers, 0 validation errors — counts unchanged.

## Validation history
| When | Schema errors | Rows in / entries out | Notes |
|---|---|---|---|
| 2026-06-28 | 0 | 266 rows / 22 entries (3 sections) | First run — CLEAN |
| 2026-06-28 | 0 | 266 rows / 22 entries / 33 tiers (3 sections) | Post-DQ-fix run — CLEAN. Schema updated: Tier $def added, Entry.tiers replaces flat persyaratan/kewajiban/parameter. |
| 2026-06-28 | 0 | 266 rows / 22 entries / 33 tiers (3 sections) | Family-idiom refactor — CLEAN. List items now `{teks, sub}` (kkp-licensing canonical) instead of `{label, text, children}`; counts unchanged. |

### 2026-06-28 — OQ Answers (user confirmed)

- OQ-1: `(*berlaku untuk seluruh KBLI)` in Col B → separate field `kbli_note` on the entry
- OQ-2: Col G `parameter` singletons → always wrap as array (uniform schema)
- OQ-3: Third numbering level `^\d+\)` confirmed → add as level 3 to `nest_by_pattern` for C, E, G
- OQ-4: Col H `kewenangan` repeats same value on continuation rows → take first occurrence per entry
- OQ-5: Output structure → sections as wrapper objects (array-of-sections)
- OQ-6: `*` prefix in Col B → boolean flag `applies_to_all_kbli: true`; strip asterisk from `nama_pb_umku`

## Open items / next steps
- [x] Mapping confirmed by user
- [x] Author `table-20260628-2.schema.json` (schema-designer)
- [x] Write and run `table-20260628-2.parser.py` (parser-builder)
- [x] Validate to 0 errors
- [x] Data-quality review
- [x] Apply approved DQ fixes

## Milestones (parser build)
- [2026-06-28] [parse] Parser written and run; 3 sections, 22 entries produced. rows_seen=266, entries=22, details=241.
- [2026-06-28] [validate] validate_json.py: 0 errors — CLEAN.
- [2026-06-28] [dq] Data-quality review complete; 10 issues found (2 P1, 4 P2, 4 P3). See data-quality-table-20260628-2.md.
- [2026-06-28] [fix] Applied P1-A (tier sub-array), P1-B (nama_pb_umku no=10 recovery), P2-A (8 dehyphen + 2 additional split-variants bersang-kutan, penang-kapan), P2-B (4 typos), P3-C (kbli_note cleanup).
- [2026-06-28] [validate] Post-fix validate_json.py: 0 errors — CLEAN.
- [2026-06-28] [refactor] Warm-started to the `kkp-licensing` family canonical: list items now `{teks, sub}`; parser paths de-staled. validate_json.py: 0 errors — CLEAN; 266/22/33 unchanged.
